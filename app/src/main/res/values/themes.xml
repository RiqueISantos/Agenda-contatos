<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema principal do app -->
    <style name="Theme.AgendaDeContatos" parent="Theme.Material3.Light.NoActionBar">
        <!-- Cores principais -->
        <item name="colorPrimary">@color/primaryRed</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/purple_500</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Cor de fundo geral -->
        <item name="android:windowBackground">@color/backgroundLight</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">@color/primaryRed</item>
        <item name="android:navigationBarColor">@color/primaryRed</item>

        <!-- Fonte e aparência -->
        <item name="android:textColor">@color/textDark</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>

</resources>
